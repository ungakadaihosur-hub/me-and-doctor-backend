{
  "name": "@me-and/auth-core",
  "version": "2.0.0",
  "description": "Session JWT verification, shared across Me & products. Session issuance itself now lives in Supabase Auth (magic link email) rather than being minted by this package.",
  "main": "src/index.js",
  "dependencies": {
    "jsonwebtoken": "^9.0.2"
  }
}
